import { InputHandler } from 'src/input';

/**
 * State is in charge of receiving all inputs
 * and respond to them.
 *
 * @export
 * @interface State
 */
export interface State extends InputHandler {
}

